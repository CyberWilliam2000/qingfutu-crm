# 青蓝图 · 客户追踪看板 部署记录

## 项目信息

| 项目 | 值 |
|---|---|
| 项目名称 | 青蓝图客户追踪看板 |
| 功能 | 客户跟进看板，看板编辑自动同步 Excel |

## 服务器信息

| 项目 | 值 |
|---|---|
| 服务器 IP | 81.70.202.143 |
| 服务器系统 | Ubuntu (轻量应用服务器) |
| SSH 用户 | ubuntu (密码登录) |
| 部署路径 | /home/ubuntu/qingfutu/ |
| 主域名 | beneblue.co (公司官网，不动) |
| 看板子域名 | dashboard.beneblue.co |
| 看板登录 | HTTP Basic Auth |

## 服务架构

```
浏览器 → dashboard.beneblue.co:80 → Nginx (密码验证) → 127.0.0.1:5000 (gunicorn + Flask)
                                                                         ↓
                                                                  客户追踪看板.xlsx
```

## 服务器文件清单

```
/home/ubuntu/qingfutu/
├── api_server.py          # Flask 后端 (API + 静态文件)
├── 客户追踪看板.html       # 看板前端页面
├── 客户追踪看板.xlsx       # Excel 数据文件
├── 启动看板.sh             # 本地启动脚本 (备用)
├── 启动看板.bat            # 本地启动脚本 (备用)
└── venv/                   # Python 虚拟环境
```

## 服务管理命令

```bash
# 查看服务状态
sudo systemctl status qingfutu-crm

# 重启服务
sudo systemctl restart qingfutu-crm

# 查看日志
sudo journalctl -u qingfutu-crm -f

# Nginx 重载
sudo systemctl reload nginx
```

## 账号密码

| 用途 | 用户名 | 密码 |
|---|---|---|
| SSH 登录 | ubuntu | (重置后的密码) |
| 看板登录 | (待设置) | (待设置) |

## 关键操作记录

### 1. 开启 SSH 密码登录
- 腾讯云 VNC 登录
- `sed -i 's/^#*PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config`
- `systemctl restart sshd`

### 2. 下载部署包
- 从沙箱临时 URL 下载 zip
- 解压到 `~/qingfutu`

### 3. 安装依赖
```bash
cd ~/qingfutu
python3 -m venv venv
source venv/bin/activate
pip install flask flask-cors openpyxl gunicorn
```

### 4. 启动服务
```bash
gunicorn -b 0.0.0.0:5000 api_server:app
```

### 5. Nginx 配置 (子域名 + 密码保护)
```bash
# 安装 htpasswd
sudo apt install -y apache2-utils

# 创建看板登录账号
sudo htpasswd -c /etc/nginx/.htpasswd-qingfutu <用户名>

# 配置 Nginx
# 文件: /etc/nginx/sites-available/qingfutu
# 域名: dashboard.beneblue.co
```

### 6. Systemd 自启
```bash
# 服务文件: /etc/systemd/system/qingfutu-crm.service
sudo systemctl daemon-reload
sudo systemctl enable qingfutu-crm
sudo systemctl start qingfutu-crm
```

## DNS 解析

| 主机记录 | 类型 | 值 | 说明 |
|---|---|---|---|
| @ | A | (原IP) | 公司官网，不动 |
| dashboard | A | 81.70.202.143 | 看板子域名 |

## 注意事项

1. 不要动 beneblue.co 主页的 A 记录
2. 看板数据文件是 `/home/ubuntu/qingfutu/客户追踪看板.xlsx`，可以备份
3. 看板中编辑自动同步到 Excel
4. 需要 HTTPS 的话，后续可以用 certbot 加
