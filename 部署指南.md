# 客户追踪看板 · 服务器部署指南

## 前提
- 服务器已装 Python 3.8+（`python3 --version` 确认）
- 已解析域名指向服务器 IP
- 80/443 端口未被占用（用 Nginx 反代）

---

## 快速部署（一条命令）

SSH 登录服务器后执行：

```bash
# 1. 创建目录
mkdir -p /opt/qingfutu-crm && cd /opt/qingfutu-crm

# 2. 安装依赖
pip3 install flask flask-cors openpyxl gunicorn

# 3. 上传文件（在你本地电脑终端执行，不是在服务器）
# scp api_server.py 客户追踪看板.html 客户追踪看板.xlsx root@你的服务器IP:/opt/qingfutu-crm/

# 4. 启动（先测试）
cd /opt/qingfutu-crm
gunicorn -b 0.0.0.0:5000 api_server:app --daemon

# 5. 验证
curl http://127.0.0.1:5000/api/health
```

---

## Nginx 反代（绑定域名）

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

启用：
```bash
nginx -t && nginx -s reload
```

---

## 设置开机自启（systemd）

```bash
cat > /etc/systemd/system/qingfutu-crm.service <<'EOF'
[Unit]
Description=青蓝图客户追踪看板
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/qingfutu-crm
ExecStart=/usr/local/bin/gunicorn -b 0.0.0.0:5000 api_server:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable qingfutu-crm
systemctl start qingfutu-crm
systemctl status qingfutu-crm
```

---

## HTTPS（可选，用 Let's Encrypt）

```bash
apt install certbot python3-certbot-nginx
certbot --nginx -d 你的域名.com
```

---

## 更新数据

看板的数据文件是 `/opt/qingfutu-crm/客户追踪看板.xlsx`。
- 在线编辑看板 → 自动写回这个 Excel
- 也可以直接替换这个 Excel 文件（保留同名），然后点「从 Excel 加载」

---

## 一句话总结

1. 把 3 个文件上传到服务器
2. 跑 `gunicorn`
3. 配 Nginx 反代
4. 域名访问，完事
